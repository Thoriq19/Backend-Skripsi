<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    /**
     * Seed the application's database.
     *
     * Note: This service uses a shared database.
     * All seeding is handled centrally via auth-service.
     * See: database/seeders/DatabaseSeeder.php (project root)
     */
    public function run(): void
    {
        // Seeding is handled centrally. Do not add seeds here.
    }
}
